<p id="notice"><%= notice %></p>

<h1>Parameters</h1>

<table>
  <thead>
    <tr>
      <th>Lang</th>
      <th>Consumer key</th>
      <th>Consumer secret</th>
      <th>Access token</th>
      <th>Access token secret</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parameters.each do |parameter| %>
      <tr>
        <td><%= parameter.lang %></td>
        <td><%= parameter.consumer_key %></td>
        <td><%= parameter.consumer_secret %></td>
        <td><%= parameter.access_token %></td>
        <td><%= parameter.access_token_secret %></td>
        <td><%= link_to 'Show', parameter %></td>
        <td><%= link_to 'Edit', edit_parameter_path(parameter) %></td>
        <td><%= link_to 'Destroy', parameter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Parameter', new_parameter_path %>
